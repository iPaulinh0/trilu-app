Small uppercase status pill for metadata (`MISSÃO DE HOJE`, `NOVO RECORDE`).

```jsx
<Badge tone="sun">Novo recorde</Badge>
```

Uppercase + wide tracking is fine here — the brand kit only forbids caps in long text.