The default Trilu surface: 20px radius, soft ambient shadow, no border.

```jsx
<Card tone="celebrate"><h3>Marco de 7 dias</h3></Card>
```

Tones map to the semantic palette — `accent` for goals, `success` for completed, `celebrate` for achievements. Never combine a tone with a colored left border.