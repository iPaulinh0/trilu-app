Square-tap, circular icon-only button for headers and toolbars; always pass `label`.

```jsx
<IconButton variant="card" label="Notificações" icon={<i data-lucide="bell" />} />
```

Sizes 36/44/52px — never below 44px inside the app UI kit.